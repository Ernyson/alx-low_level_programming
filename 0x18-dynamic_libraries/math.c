int add(int a, int b)
{
int sum;

sum = a + b;
return (sum);
}

int sub(int a, int b)
{
int sum;

sum = a - b;
return (sum);
}

int mul(int a, int b)
{
int sum;

sum = a * b;
return (sum);
}

int div(int a, int b)
{
int sum;

sum = a / b;
return (sum);
}

int mod(int a, int b)
{
int sum;

sum = a % b;
return (sum);
}
